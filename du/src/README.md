# du command

`Command` owns NUL-input handling, diagnostics, output selection, totals, thresholds, timestamps, and stream termination. `DuOptionParser` provides the GNU-style option vocabulary. Recursive accounting is delegated to `Icod.CoreUtils.Shared.FileSystem.Usage.DiskUsageCalculator`.
