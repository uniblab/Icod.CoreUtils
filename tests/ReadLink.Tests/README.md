# ReadLink tests

The test project covers direct link inspection, all canonicalization modes, delimiters, quiet/verbose policy, multiple-operand continuation, loops, strict trailing separators, and cancellation over a deterministic POSIX filesystem model.
