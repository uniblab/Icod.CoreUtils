# RealPath tests

The test project covers physical, logical, and no-link resolution; all missing-component policies; relative output; delimiters; quiet diagnostics; continuation; loops; strict trailing separators; and cancellation over a deterministic POSIX filesystem model.
