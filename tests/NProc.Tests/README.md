# NProc.Tests

These tests keep GNU command policy separate from platform observation. Injected
F2 snapshots cover affinity, managed process availability, online and installed
fallbacks, fractional quotas, OpenMP environment overrides, `--all`, `--ignore`,
and the mandatory minimum of one. Command-boundary tests cover parsing,
informational output, diagnostics, and provider failures.
