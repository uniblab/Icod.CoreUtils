# Delimiter and separator tests

`DelimiterTests.cs` covers immutable input copying, the required distinction between nonempty match delimiters and possibly empty output separators, repeating separator cycles, cycle validation, multibyte incremental matching, overlapping matches, mismatch fallback, and explicit reset behavior.
