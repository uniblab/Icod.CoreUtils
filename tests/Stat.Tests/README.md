# stat tests

The suite covers argument diagnostics, help and version behavior, authoritative file metadata, directory reporting, GNU file and filesystem format strings, field width and precision behavior, numeric flags, `--printf` escapes, terse reports, Linux `dev_t` encoding, negative fractional epochs, link dereferencing where supported, and continued processing after an operand failure.
