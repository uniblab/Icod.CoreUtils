name: pr-build-and-test

on:
  pull_request:

jobs:
  build-and-test:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - run: dotnet clean Icod.CoreUtils.sln -c Debug
      - run: dotnet restore Icod.CoreUtils.sln
      - run: dotnet build Icod.CoreUtils.sln -c Debug --no-restore
      - run: dotnet test Icod.CoreUtils.sln -c Debug --no-build --logger trx
      - name: Validate isolated Coreutils boundary
        shell: pwsh
        run: ./eng/validate-g3n-coreutils.ps1 -Configuration Debug
