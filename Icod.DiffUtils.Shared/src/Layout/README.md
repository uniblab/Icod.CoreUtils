# Shared layout primitives

This directory contains logical side-by-side rows and display fitting used by both `diff --side-by-side` and the later `sdiff` command.

Terminal interaction, editor execution, transactional output, and command-specific formatting remain outside this shared directory.
